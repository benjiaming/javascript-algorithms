{
    "name": "algorithms",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "test": "jest --watch"
    },
    "devDependencies": {
        "@babel/preset-env": "^7.4.5",
        "babel-jest": "^24.8.0",
        "jest": "^24.8.0"
    },
    "jest": {
        "transform": {
            "^.+\\.jsx?$": "babel-jest"
        }
    }
}
